@model EmployeeManagement.DTOs.EmployeeEditDTO

@{
    ViewData["Title"] = "Edit Employee";
    var currentPage = ViewBag.CurrentPage;
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Edit Employee</h2>
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="EmployeeId" />

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label"></label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" asp-route-page="@currentPage" class="btn btn-primary">Save Changes</button>
                        </div>
                    </form>
                    <div class="mt-3 text-center">
                        <a asp-action="AllEmployees" asp-route-page="@currentPage" class="btn btn-secondary">Go Back To Employees</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
